# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log

# Node / Vite (frontend má vlastný .gitignore — toto je redundantná poistka)
node_modules/
dist/
build/
.vite/

# PHP / Composer
vendor/

# Env súbory — NIKDY necommituj
.env
.env.local
.env.*.local

# AI assistant scratch + config — never tracked, the directory may
# contain machine-specific paths and design notes that don't belong
# in version control.
.claude/

# Veľké materiály k handoffu (referenčné, nie v repe)
firol.pdf
handoff.zip
docs/*
# …ale roadmap je tracked (referencuje sa z CLAUDE.md)
!docs/development-roadmap.md
opravy-pdf/

# Deploy artefakty (zostavujú sa v CI)
deploy/

# Local working files (audits, notes, plans, scratch) — not for production
AUDIT.md

# Project context for AI assistants — kept local so the repo doesn't
# advertise tooling. Live copy stays on developer machines.
CLAUDE.md

