# Frontend env premenné. Skopíruj do .env.local pre lokálny vývoj.
# V kóde dostupné cez import.meta.env.VITE_*

# URL backend API (v dev režime stačí "/api" — Vite proxy si to prevezme).
VITE_API_BASE_URL=/api
