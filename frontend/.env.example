# Frontend env premenné.
# Pre lokálny vývoj skopíruj do .env.local a nastav na prázdne (Vite proxy sa postará o /api/*).
# Na produkcii (bez mod_rewrite) sa nastaví automaticky cez .env.production na /index.php.

VITE_API_BASE_URL=
