# Reflects the Origin header back only for local Vite dev servers, whatever
# port they happen to be on (Vite bumps the port when 5173 is already taken
# by another project). Dev-only — production doesn't use this nginx config.
map $http_origin $cors_origin {
    default "";
    "~^http://localhost:[0-9]+$" $http_origin;
}
